# Dépendances du venv utilisé par run_python_file (serveur MCP python, mcp_python_server.py).
# Le code exécuté par le LLM via run_python_file tourne dans ce venv : garder ces versions à jour
# si vous ajoutez des bibliothèques.
#
# Recréer le venv depuis zéro :
#   uv venv workdir/.venv --python 3.12
#   uv pip install --python workdir/.venv/bin/python -r workdir/requirements.txt

sympy==1.14.0
numpy==2.4.4
scipy==1.17.1
matplotlib==3.10.9
pandas==3.0.2
requests==2.33.1
